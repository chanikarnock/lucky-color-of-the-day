import { useState } from "react"



function DatePickerElm () {
    const [selectedDate, setSelectedDate] = useState<Date | null>(new Date())
    return (
        <div></div>
        
    );
}

export default DatePickerElm;